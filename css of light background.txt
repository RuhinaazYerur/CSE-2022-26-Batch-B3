:root{
  /* Neutral + calm */
  --text: rgba(17, 24, 39, 0.92);
  --muted: rgba(17, 24, 39, 0.62);

  /* Glass (softer) */
  --card: rgba(255,255,255,0.70);
  --card2: rgba(255,255,255,0.56);
  --stroke: rgba(15, 23, 42, 0.06);
  --stroke2: rgba(15, 23, 42, 0.08);

  /* Much softer shadows */
  --shadow: 0 10px 28px rgba(2, 6, 23, 0.08);
  --shadow2: 0 8px 18px rgba(2, 6, 23, 0.06);
  --radius: 20px;

  /* ✅ Calm accent (no purple/blue gradients) */
  --a1: #0f766e;      /* teal (classy) */
  --a2: #0ea5a4;      /* soft teal */
  --a3: #16a34a;      /* green (minimal use) */
  --a4: #ea580c;      /* orange (rare use) */

  /* Very subtle glow */
  --glowA: 0 0 10px rgba(14,165,164,0.10);
  --ring: 0 0 0 3px rgba(14,165,164,0.14);
}

*{ box-sizing: border-box; }
html,body{ height:100%; }

body{
  margin:0;
  font-family: "Poppins", system-ui, -apple-system, Segoe UI, Roboto, Arial, sans-serif;
  color: var(--text);
  overflow-x:hidden;
  background: #f7f8fb;
}

/* ✅ Calm background (almost plain, just soft depth) */
.bg{
  position: fixed;
  inset: 0;
  background:
    radial-gradient(900px 520px at 18% 18%, rgba(14,165,164,0.07), transparent 64%),
    radial-gradient(900px 520px at 82% 22%, rgba(15,118,110,0.05), transparent 66%),
    linear-gradient(180deg, #f7f8fb 0%, #f2f4f8 55%, #f7f8fb 100%);
  z-index: -2;
}

.overlay{
  position: fixed;
  inset: 0;
  background: rgba(255,255,255,0.14);
  z-index: -1;
}

/* Navbar */
.nav{
  position: sticky;
  top: 0;
  z-index: 20;
  background: rgba(255,255,255,0.74);
  border-bottom: 1px solid rgba(15,23,42,0.06);
  backdrop-filter: blur(12px);
}

.nav__inner{
  max-width: 1150px;
  margin: 0 auto;
  padding: 14px 18px;
  display:flex;
  align-items:center;
  justify-content: space-between;
  gap: 14px;
}

.brand{
  display:flex;
  align-items:center;
  gap: 10px;
  text-decoration: none;
  color: var(--text);
}

.brand__dot{
  width: 12px; height: 12px;
  border-radius: 999px;
  background: linear-gradient(135deg, var(--a1), var(--a2));
  box-shadow: var(--glowA);
}
.brand__text{ font-weight: 800; letter-spacing: .2px; }

.links{ display:flex; gap: 10px; flex-wrap: wrap; }
.link{
  text-decoration:none;
  color: rgba(17,24,39,0.70);
  padding: 8px 12px;
  border-radius: 999px;
  transition: 0.2s ease;
  border: 1px solid transparent;
}
.link:hover{
  color: rgba(17,24,39,0.92);
  background: rgba(255,255,255,0.65);
  border-color: rgba(15,23,42,0.06);
}

/* Layout */
.page{
  max-width: 1150px;
  margin: 0 auto;
  padding: 26px 18px 46px;
}

.footer{
  padding: 18px 18px 28px;
}
.footer__inner{
  max-width: 1150px;
  margin: 0 auto;
  color: rgba(17,24,39,0.55);
  display:flex;
  gap: 10px;
  justify-content: center;
  align-items:center;
  flex-wrap: wrap;
}

/* Glass card */
.glass{
  background: linear-gradient(180deg, var(--card) 0%, var(--card2) 100%);
  border: 1px solid var(--stroke);
  box-shadow: var(--shadow);
  backdrop-filter: blur(14px);
}

.card{
  border-radius: var(--radius);
  padding: 22px;
  position: relative;
  overflow: hidden;
}

/* Remove heavy decorative blobs */
.card::before,
.card::after{
  content:none;
}

/* Lift (reduced) */
.lift{
  transform: translateY(0);
  transition: transform 0.22s ease, box-shadow 0.22s ease;
}
.lift:hover{
  transform: translateY(-3px);
  box-shadow: 0 14px 34px rgba(2,6,23,0.10);
}

.chip{
  display:inline-flex;
  align-items:center;
  gap: 8px;
  font-size: 12px;
  color: rgba(17,24,39,0.70);
  padding: 8px 12px;
  border-radius: 999px;
  background: rgba(255,255,255,0.70);
  border: 1px solid rgba(15,23,42,0.06);
}

/* Typography */
.h1{
  font-size: 42px;
  line-height: 1.10;
  margin: 14px 0 10px;
}
.h2{ font-size: 22px; margin: 10px 0 8px; }
.h3{ font-size: 16px; margin: 0 0 10px; }

.sub{ color: var(--muted); max-width: 560px; }
.muted{ color: var(--muted); }

/* Accent text (subtle, not gradient-heavy) */
.accent{
  color: var(--a1);
}

/* Buttons */
.btn{
  border: 1px solid rgba(15,23,42,0.08);
  border-radius: 14px;
  padding: 12px 14px;
  font-weight: 800;
  cursor: pointer;
  text-decoration:none;
  display:inline-flex;
  align-items:center;
  justify-content:center;
  gap: 10px;
  transition: 0.2s ease;
  color: rgba(17,24,39,0.90);
  background: rgba(255,255,255,0.70);
  box-shadow: 0 8px 18px rgba(2,6,23,0.06);
}
.btn:hover{
  transform: translateY(-1px);
  box-shadow: 0 10px 22px rgba(2,6,23,0.08);
}

.btn.primary{
  width: 100%;
  border: 0;
  color: white;
  background: linear-gradient(135deg, var(--a1), var(--a2));
  box-shadow: 0 12px 26px rgba(14,165,164,0.16);
}
.btn.primary:hover{ filter: brightness(1.02); }

.btn.ghost{
  background: rgba(255,255,255,0.62);
}

/* Hero */
.hero{
  display:grid;
  grid-template-columns: 1.2fr 1fr;
  gap: 22px;
  align-items: start;
  animation: fadeUp 0.45s ease both;
}
@media (max-width: 980px){
  .hero{ grid-template-columns: 1fr; }
  .h1{ font-size: 34px; }
}

/* Stats */
.stats{
  margin-top: 18px;
  display:flex;
  gap: 12px;
  flex-wrap: wrap;
}
.stat{
  padding: 14px 16px;
  border-radius: 18px;
  background: rgba(255,255,255,0.66);
  border: 1px solid rgba(15,23,42,0.06);
  box-shadow: var(--shadow2);
}
.stat__num{ font-weight: 900; font-size: 16px; }
.stat__label{ color: rgba(17,24,39,0.62); font-size: 12px; }

/* Form */
.form{ margin-top: 14px; display:flex; flex-direction: column; gap: 12px; }

.drop{
  border-radius: 18px;
  border: 1px dashed rgba(15,23,42,0.16);
  background: rgba(255,255,255,0.64);
  padding: 16px;
  display:flex;
  gap: 14px;
  align-items:center;
  cursor: pointer;
  transition: 0.2s ease;
}
.drop:hover{
  background: rgba(255,255,255,0.78);
  box-shadow: 0 10px 22px rgba(2,6,23,0.06);
}
.drop input{ display:none; }

.drop__icon{
  width: 42px; height: 42px;
  border-radius: 14px;
  display:flex;
  align-items:center;
  justify-content:center;
  background: rgba(14,165,164,0.10);
  border: 1px solid rgba(15,23,42,0.06);
}
.drop__title{ font-weight: 800; }
.drop__sub{ color: rgba(17,24,39,0.62); font-size: 12px; }

.drop.dragover{
  border-color: rgba(14,165,164,0.55);
  background: rgba(14,165,164,0.08);
  box-shadow: var(--glowA);
  transform: translateY(-1px);
}

.preview{
  display:none;
  border-radius: 18px;
  overflow:hidden;
  border: 1px solid rgba(15,23,42,0.06);
  background: rgba(255,255,255,0.62);
  cursor: pointer;
}
.preview.show{ display:block; animation: fadeUp 0.35s ease both; }
.preview img{
  width: 100%;
  height: 240px;
  object-fit: cover;
  display:block;
}
.preview__meta{
  padding: 10px 12px;
  color: rgba(17,24,39,0.70);
  font-size: 12px;
  display:flex;
  align-items:center;
  justify-content: space-between;
  gap: 10px;
}

/* Tip */
.tip{
  margin-top: 12px;
  color: rgba(17,24,39,0.60);
  font-size: 12px;
}

/* Result */
.result{ animation: fadeUp 0.45s ease both; }

.result__grid{
  margin-top: 10px;
  display:grid;
  grid-template-columns: 1fr 1fr;
  gap: 16px;
}
@media (max-width: 980px){
  .result__grid{ grid-template-columns: 1fr; }
}

.result__top{
  display:flex;
  justify-content: space-between;
  align-items: center;
  gap: 12px;
  flex-wrap: wrap;
  margin-bottom: 10px;
}

.bar{
  margin-top: 12px;
  height: 12px;
  border-radius: 999px;
  background: rgba(15,23,42,0.10);
  overflow: hidden;
}
.bar__fill{
  height: 100%;
  width: 0%;
  background: linear-gradient(90deg, var(--a1), var(--a2));
  border-radius: 999px;
  animation: fill 0.7s ease both;
}

.imgbox{
  border-radius: 18px;
  overflow:hidden;
  border: 1px solid rgba(15,23,42,0.06);
  background: rgba(255,255,255,0.60);
}
.imgbox img{
  width: 100%;
  height: 420px;
  object-fit: cover;
  display:block;
}

.panel{
  padding: 14px;
  border-radius: 18px;
  background: rgba(255,255,255,0.64);
  border: 1px solid rgba(15,23,42,0.06);
  box-shadow: var(--shadow2);
}

.prob__row{
  display:flex;
  justify-content: space-between;
  gap: 10px;
  font-size: 13px;
}
.prob__name{ color: rgba(17,24,39,0.84); }
.prob__val{ color: rgba(17,24,39,0.60); }

.prob__bar{
  height: 10px;
  border-radius: 999px;
  background: rgba(15,23,42,0.10);
  overflow:hidden;
  margin-top: 6px;
}
.prob__barFill{
  height: 100%;
  background: linear-gradient(90deg, var(--a2), var(--a3));
  border-radius: 999px;
}

/* Simple pages */
.simple{
  max-width: 980px;
  margin: 0 auto;
  animation: fadeUp 0.45s ease both;
}
.text{ color: rgba(17,24,39,0.78); line-height: 1.7; }

.grid2{
  margin-top: 14px;
  display:grid;
  grid-template-columns: 1fr 1fr;
  gap: 12px;
}
@media (max-width: 900px){
  .grid2{ grid-template-columns: 1fr; }
}
.grid3{
  margin-top: 14px;
  display:grid;
  grid-template-columns: 1fr 1fr 1fr;
  gap: 12px;
}
@media (max-width: 900px){
  .grid3{ grid-template-columns: 1fr; }
}

.mini{
  padding: 14px;
  border-radius: 18px;
  background: rgba(255,255,255,0.66);
  border: 1px solid rgba(15,23,42,0.06);
  box-shadow: var(--shadow2);
  transition: transform 0.25s ease, box-shadow 0.25s ease;
}
.mini:hover{
  transform: translateY(-6px);
  box-shadow: 0 14px 28px rgba(2,6,23,0.10);
}

.center{ text-align:center; }
.big{ font-size: 22px; font-weight: 900; }

.list{
  margin: 0;
  padding-left: 18px;
  color: rgba(17,24,39,0.78);
}
.list li{ margin: 8px 0; }

/* Dashboard cards (make them glass, not heavy color blocks) */
.dash-cards{
  display:grid;
  grid-template-columns:repeat(4,1fr);
  gap:15px;
  margin-bottom:20px;
}
@media (max-width: 980px){
  .dash-cards{ grid-template-columns: repeat(2,1fr); }
}
@media (max-width: 520px){
  .dash-cards{ grid-template-columns: 1fr; }
}
.dash-card{
  padding: 16px;
  border-radius: 18px;
  text-align:center;
  background: rgba(255,255,255,0.70);
  border: 1px solid rgba(15,23,42,0.06);
  box-shadow: var(--shadow2);
  transition: transform .22s ease, box-shadow .22s ease;
}
.dash-card:hover{
  transform: translateY(-5px);
  box-shadow: 0 14px 28px rgba(2,6,23,0.10);
}
.dash-title{ font-size:13px; opacity:.80; color: rgba(17,24,39,0.62); }
.dash-value{ font-size:18px; font-weight:900; margin-top:6px; color: rgba(17,24,39,0.92); }

/* FAQ */
.faq{
  margin-top:20px;
  display:flex;
  flex-direction:column;
  gap:12px;
}
.faq-item{
  border-radius:18px;
  background: rgba(255,255,255,0.70);
  border:1px solid rgba(15,23,42,0.06);
  box-shadow: var(--shadow2);
  overflow:hidden;
  transition: all .22s ease;
}
.faq-question{
  width:100%;
  padding:16px 20px;
  background:none;
  border:none;
  font-weight:700;
  font-size:14px;
  cursor:pointer;
  display:flex;
  justify-content:space-between;
  align-items:center;
}
.faq-question:hover{
  background: rgba(255,255,255,0.82);
}
.faq-icon{
  font-size:18px;
  font-weight:800;
  transition: transform .25s ease;
  color: rgba(17,24,39,0.70);
}
.faq-answer{
  max-height:0;
  overflow:hidden;
  padding:0 20px;
  font-size:13px;
  color:var(--muted);
  line-height:1.6;
  transition:max-height .35s ease, padding .25s ease;
}
.faq-item.active .faq-answer{
  max-height:300px;
  padding:0 20px 18px;
}
.faq-item.active .faq-icon{
  transform: rotate(45deg);
}
.faq-item.active{
  box-shadow: 0 14px 30px rgba(2,6,23,0.10), var(--glowA);
}

/* Tables (performance page) */
.table{
  width:100%;
  border-collapse: collapse;
  font-size: 13px;
}
.table th, .table td{
  padding: 10px 12px;
  border-bottom: 1px solid rgba(15,23,42,0.08);
  text-align: left;
  white-space: nowrap;
}
.table thead th{
  background: rgba(255,255,255,0.70);
  font-weight: 800;
}
.table tbody tr:hover{
  background: rgba(255,255,255,0.60);
}

/* Performance plot sizing */
.simple .imgbox img{
  height: 260px;
  object-fit: contain;
  background: rgba(255,255,255,0.40);
}

/* Focus states */
a:focus, button:focus, input:focus, label:focus{ outline: none; }
a:focus-visible, button:focus-visible, input:focus-visible, label:focus-visible{
  box-shadow: var(--ring);
  border-radius: 12px;
}

/* Animations */
@keyframes fill{ from { width: 0%; } }
@keyframes fadeUp{
  from{ opacity:0; transform: translateY(10px); }
  to{ opacity:1; transform: translateY(0); }
}

/* Reduce motion support */
@media (prefers-reduced-motion: reduce){
  *{ animation: none !important; transition: none !important; }
}
